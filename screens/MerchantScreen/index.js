import React, { Component } from 'react';
import { Text, View } from 'react-native';

import MerchantQueue from '../../components/MerchantQueue/MerchantQueue'

export default class MerchantScreen extends Component {
  render() {
    return ( <MerchantQueue /> );
  }
}